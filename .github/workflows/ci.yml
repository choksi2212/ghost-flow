name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    # CI is informational only - failures won't block merges
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        rust: [stable]
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: ${{ matrix.rust }}
        override: true
        components: rustfmt, clippy
    
    - name: Cache cargo registry
      uses: actions/cache@v3
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
    
    - name: Cache cargo index
      uses: actions/cache@v3
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
    
    - name: Cache cargo build
      uses: actions/cache@v3
      with:
        path: target
        key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
    
    - name: Check formatting
      continue-on-error: true
      run: cargo fmt --all -- --check
    
    - name: Run clippy
      continue-on-error: true
      run: cargo clippy --workspace --all-targets --all-features -- -D warnings
    
    - name: Build
      continue-on-error: true
      run: cargo build --workspace --verbose
    
    - name: Run tests
      continue-on-error: true
      run: cargo test --workspace --verbose
    
    - name: Run doc tests
      continue-on-error: true
      run: cargo test --workspace --doc --verbose

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    # Temporarily disabled due to pulp dependency issue
    if: false
    steps:
    - uses: actions/checkout@v3
    
    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
        override: true
    
    - name: Install tarpaulin
      run: cargo install cargo-tarpaulin
    
    - name: Generate coverage
      run: cargo tarpaulin --workspace --out Xml --verbose
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: ./cobertura.xml
        fail_ci_if_error: false

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    # Informational only - won't block builds
    continue-on-error: true
    steps:
    - uses: actions/checkout@v3
    
    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
        override: true
    
    - name: Install cargo-audit
      run: cargo install cargo-audit
    
    - name: Run security audit
      continue-on-error: true
      run: cargo audit

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    # Informational only - won't block builds
    continue-on-error: true
    steps:
    - uses: actions/checkout@v3
    
    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
        override: true
    
    - name: Build documentation
      continue-on-error: true
      run: cargo doc --workspace --no-deps --all-features
    
    - name: Check for broken links
      continue-on-error: true
      run: cargo doc --workspace --no-deps --all-features 2>&1 | grep -i "warning.*broken" && exit 1 || exit 0
